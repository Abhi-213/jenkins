pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Add build commands here, like 'mvn clean install' for Maven projects
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Add test commands here, such as 'npm test' for Node.js projects
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Add deploy commands here, or integration steps
            }
        }
    }
}
